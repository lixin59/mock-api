export enum HTTPStatusCodeEnum {
  Continue = 100,
  SwitchingProtocols = 101,
  OK = 200,
  BadRequest = 400,
  Unauthorized = 401,
  Forbidden = 403,
  NotFound = 404,
  InternalServerError = 500
}
